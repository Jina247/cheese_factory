<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CheeseFactory" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CheeseFactory" parent="Base.Theme.CheeseFactory" />

    <style name="MyFilterChip" parent="Widget.Material3.Chip.Filter">
        <item name="chipStrokeWidth">0.5dp</item>
        <item name="chipCornerRadius">24dp</item>
        <item name="fontFamily">@font/poppins</item>
        <item name="chipStrokeColor">@color/timber</item>
        <item name="chipBackgroundColor">@color/selector_chip_bg</item>
        <item name="android:textColor">@color/selector_chip_text</item>
        <item name="checkedIconVisible">true</item>
        <item name="checkedIconTint">@color/white</item>
    </style>


</resources>